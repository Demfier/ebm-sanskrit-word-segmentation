gaurav folder contains scripts used in the EBM Word Segmentation task.

The scripts here are used for:

1. Generating metapaths (of size 8L x 8L)
2. Feature Reduction and Selection task

It also contains some other scripts from previous work of PCRW and some matrices.

The EBM_code folder contains the pipeline code
